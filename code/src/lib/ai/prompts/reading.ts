// ==========================================
// 3축 해석 프롬프트
// ==========================================
//
// [이 파일이 하는 일]
// daily.ts에서 이미 3축(연애/일/재물) 해석을 포함한 전체 프롬프트를 정의했으므로,
// 이 파일은 단순히 daily.ts의 내용을 re-export합니다.
//
// [왜 별도 파일이 있나?]
// 향후 3축 각각을 별도로 호출해야 할 경우를 대비한 구조입니다.
// 예: 연애운만 따로 깊이 분석하는 기능이 추가될 때
// 현재는 daily.ts에서 한 번에 처리하므로 이 파일은 진입점 역할만 합니다.

export { DAILY_SYSTEM_PROMPT, buildDailyUserPrompt } from './daily';
